{
  "name": "webedt-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "WebEDT - AI-powered code editing platform",
  "workspaces": [
    "shared",
    "cli",
    "website/frontend",
    "website/backend",
    "tools/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "concurrently -n shared,frontend,backend -c magenta,blue,green \"npm run dev -w shared\" \"npm run dev -w website/frontend\" \"npm run dev -w website/backend\"",
    "start": "concurrently -n frontend,backend -c blue,green \"npm run preview -w website/frontend\" \"npm run start -w website/backend\"",
    "clean": "npm run clean --workspaces --if-present",
    "cli": "npm run dev -w cli --"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
