# Database connection (same as internal-api-server)
DATABASE_URL=postgresql://user:password@localhost:5432/webedt

# User email to lookup credentials from database
USER_EMAIL=etdofresh@gmail.com

# Target repository
REPO_OWNER=webedt
REPO_NAME=monorepo
REPO_BASE_BRANCH=main

# Optional: Override credentials (if not using database)
# GITHUB_TOKEN=ghp_xxx
# CLAUDE_ACCESS_TOKEN=sk-ant-oat01-xxx
# CLAUDE_REFRESH_TOKEN=sk-ant-ort01-xxx

# Execution settings
PARALLEL_WORKERS=4
TASKS_PER_CYCLE=5
WORK_DIR=/tmp/autonomous-dev

# Evaluation settings
PREVIEW_URL_PATTERN=https://github.etdofresh.com/{owner}/{repo}/{branch}/

# Daemon settings
LOOP_INTERVAL_MS=60000
