/**
 * Audio Library
 * Exports audio source and related utilities
 */

export { AudioSource } from './audioSource';
export type {
  AudioSourceConfig,
  AudioSourceState,
  WaveformType,
  EnvelopeConfig,
  AudioSourceEvent,
  NoteInfo,
  AudioSourcePreset,
} from './types';
export { AUDIO_PRESETS, NOTE_FREQUENCIES } from './types';
