{
  "name": "website-client",
  "version": "1.0.0",
  "type": "module",
  "browserslist": [
    "Chrome >= 120",
    "Firefox >= 115",
    "Safari >= 16",
    "Edge >= 120",
    "iOS >= 16",
    "ChromeAndroid >= 120"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^6.4.1",
    "vite-plugin-version-mark": "^0.2.2"
  }
}
